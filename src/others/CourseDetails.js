import React from 'react';

const CourseDetails = ({ course }) => {
    console.log(course)

    return (
        <div>
            <h1>hello</h1>
        </div>
    );
};

export default CourseDetails;